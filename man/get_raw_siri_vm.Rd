% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_raw_siri_vm.R
\name{get_raw_siri_vm}
\alias{get_raw_siri_vm}
\title{Get real-time vehicle locations from the BODS SIRI-VM API}
\usage{
get_raw_siri_vm(
  api_key = Sys.getenv("BODS_KEY"),
  min_lat = NULL,
  min_lon = NULL,
  max_lat = NULL,
  max_lon = NULL,
  operator_ref = NULL,
  line_ref = NULL,
  vehicle_ref = NULL
)
}
\arguments{
\item{api_key}{API key for the BODS dataset. Defaults to the value of
the \code{BODS_KEY} environment variable.}

\item{min_lat}{latitude of lower left corner of bounding box
Note that bounding box parameters are specified as latitude/longitude pairs
(bottom left corner first, then top right), which follows the human
geographic convention. Internally these are converted to the longitude-first
ordering required by the BODS API (following the GeoJSON convention).}

\item{min_lon}{longitude of lower left corner of bounding box}

\item{max_lat}{latitude of upper right corner of bounding box}

\item{max_lon}{longitude of upper right corner of bounding box}

\item{operator_ref}{A string. National Operator Code (NOC) to filter results
to a specific operator. Defaults to \code{NULL}.}

\item{line_ref}{A string. Line reference to filter results to a specific
service. Defaults to \code{NULL}.}

\item{vehicle_ref}{A string. Vehicle reference to filter to a specific
vehicle. Defaults to \code{NULL}.}
}
\value{
A named list with two elements:
\describe{
\item{xml}{An \code{xml_document} object containing the raw SIRI-VM response}
\item{fetched_at}{A \code{POSIXct} timestamp recording when the response was received}
}
}
\description{
Fetches live bus vehicle location data from the Bus Open Data Service
in SIRI-VM (XML) format.
}
\examples{
\dontrun{
# All vehicles in a bounding box around South Derbyshire
xml <- get_raw_siri_vm(min_lat=52.70, min_lon=-1.75, max_lat=52.95, max_lon=-1.45)

# Filter to a specific operator
xml <- get_raw_siri_vm(
  min_lat=52.70, min_lon=-1.75, max_lat=52.95, max_lon=-1.45,
  operator_ref = "TBTN"
)
}
}
