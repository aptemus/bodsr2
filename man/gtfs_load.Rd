% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtfs_load.R
\name{gtfs_load}
\alias{gtfs_load}
\title{Load GTFS data for a region}
\usage{
gtfs_load(region = "east_midlands", download = FALSE)
}
\arguments{
\item{region}{A string. Defaults to \code{"east_midlands"}.}

\item{download}{Logical. If \code{TRUE}, forces a fresh download of the zip
regardless of cache state. Defaults to \code{FALSE}.}
}
\value{
A tidytransit object (list of tibbles).
}
\description{
Returns a tidytransit object for the specified region. Downloads the GTFS
zip from BODS if no valid cache exists, then loads and caches the result
as an RDS file for fast subsequent access. On repeated calls, the cached
RDS is returned directly without re-reading the zip.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("BODS_KEY")) || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
gtfs <- gtfs_load("east_midlands")
\dontshow{\}) # examplesIf}
}
