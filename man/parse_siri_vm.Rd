% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_siri_vm.R
\name{parse_siri_vm}
\alias{parse_siri_vm}
\title{Parse a SIRI-VM XML response into a tidy data frame}
\usage{
parse_siri_vm(result)
}
\arguments{
\item{result}{A named list as returned by \code{\link[=get_raw_siri_vm]{get_raw_siri_vm()}}, containing
elements \code{xml} (the raw SIRI-VM response) and \code{fetched_at} (the fetch
timestamp).}
}
\value{
A tibble with one row per vehicle activity record, containing
the following columns:
\describe{
\item{operator_ref}{National Operator Code (NOC)}
\item{line_ref}{Line reference as published in the feed}
\item{published_line_name}{Human-readable line name}
\item{direction}{Direction of travel: \code{"inbound"} or \code{"outbound"}}
\item{origin_ref}{ATCO code of the origin stop}
\item{origin_name}{Name of the origin stop}
\item{destination_ref}{ATCO code of the destination stop}
\item{destination_name}{Name of the destination stop}
\item{origin_aimed_departure_time}{Scheduled departure time from origin as \code{POSIXct}, \code{NA} if not published}
\item{destination_aimed_arrival_time}{Scheduled arrival time at destination as \code{POSIXct}, \code{NA} if not published}
\item{dated_vehicle_journey_ref}{Journey identifier, useful for tracking a specific journey across multiple calls}
\item{latitude}{Vehicle latitude as numeric}
\item{longitude}{Vehicle longitude as numeric}
\item{bearing}{Vehicle bearing in degrees as numeric, \code{NA} if not published}
\item{occupancy}{Passenger occupancy level as reported by the operator, \code{NA} if not published}
\item{vehicle_ref}{Vehicle reference - format varies by operator}
\item{recorded_at}{Time the vehicle position was recorded as \code{POSIXct}}
\item{valid_until_time}{Time until which this record is considered valid, as \code{POSIXct}}
\item{age_at_fetch_seconds}{Seconds elapsed between \code{recorded_at} and the time the feed was fetched.
Reflects the freshness of the operator's AVL feed at the point of retrieval.}
\item{age_seconds}{Seconds elapsed between \code{recorded_at} and the time \code{parse_siri_vm()} was called.
Use this for application logic such as deciding whether to display or dim a vehicle marker.}
}
}
\description{
Takes the raw \code{xml_document} returned by \code{\link[=get_raw_siri_vm]{get_raw_siri_vm()}} and extracts
vehicle activity records into a tidy tibble. One row per vehicle.
}
\details{
Note that field availability varies by operator. Fields such as
\code{bearing}, \code{occupancy}, and \code{destination_aimed_arrival_time} are optional
in the SIRI-VM standard and will be \code{NA} where not published. Wales is not
covered by the BODS statutory requirement and Welsh operators are unlikely
to appear in results.
}
\examples{
\dontshow{if (nzchar(Sys.getenv("BODS_KEY")) || identical(Sys.getenv("IN_PKGDOWN"), "true")) withAutoprint(\{ # examplesIf}
xml <- get_raw_siri_vm(
  api_key      = Sys.getenv("BODS_KEY"),
  min_lat      = 52.70,
  min_lon      = -1.75,
  max_lat      = 52.95,
  max_lon      = -1.45,
  operator_ref = "TBTN"
)
vehicles <- parse_siri_vm(xml)
\dontshow{\}) # examplesIf}
}
