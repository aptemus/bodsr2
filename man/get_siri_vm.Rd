% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_siri_vm.R
\name{get_siri_vm}
\alias{get_siri_vm}
\title{Get real-time vehicle locations as a tidy data frame}
\usage{
get_siri_vm(
  api_key = Sys.getenv("BODS_KEY"),
  min_lat = NULL,
  max_lat = NULL,
  min_lon = NULL,
  max_lon = NULL,
  operator_ref = NULL,
  line_ref = NULL,
  vehicle_ref = NULL
)
}
\arguments{
\item{api_key}{API key for the BODS dataset. Defaults to the value of
the \code{BODS_KEY} environment variable.}

\item{min_lat}{latitude of lower left corner of bounding box
Note that bounding box parameters are specified as latitude/longitude pairs
(bottom left corner first, then top right), which follows the human
geographic convention. Internally these are converted to the longitude-first.
ordering required by the BODS API (following the GeoJSON convention).
For large bounding boxes covering dense urban areas, consider also
supplying \code{operator_ref} to reduce response size. The BODS API
currently returns all matching vehicles in a single response with no
pagination; the minimum poll interval is 5 seconds.}

\item{max_lat}{latitude of upper right corner of bounding box}

\item{min_lon}{longitude of lower left corner of bounding box}

\item{max_lon}{longitude of upper right corner of bounding box}

\item{operator_ref}{A string. National Operator Code (NOC) to filter results
to a specific operator. Defaults to \code{NULL}.}

\item{line_ref}{A string. Line reference to filter results to a specific
service. Defaults to \code{NULL}.}

\item{vehicle_ref}{A string. Vehicle reference to filter to a specific
vehicle. Defaults to \code{NULL}.}
}
\value{
A tibble with one row per vehicle activity record. See
\code{\link[=parse_siri_vm]{parse_siri_vm()}} for a full description of columns.
}
\description{
Fetches and parses live bus vehicle location data from the Bus Open Data
Service (BODS) SIRI-VM API, returning a tidy tibble. This is the primary
interface for most users. For access to the raw XML response, see
\code{\link[=get_raw_siri_vm]{get_raw_siri_vm()}}. To parse a previously fetched response, see
\code{\link[=parse_siri_vm]{parse_siri_vm()}}.
}
\examples{
\dontrun{
# All vehicles in a bounding box around South Derbyshire
vehicles <- get_siri_vm(
  min_lat = 52.70,
  max_lat = 52.95,
  min_lon = -1.75,
  max_lon = -1.45
)

# Filter to a specific operator
vehicles <- get_siri_vm(
  min_lat      = 52.70,
  max_lat      = 52.95,
  min_lon      = -1.75,
  max_lon      = -1.45,
  operator_ref = "TBTN"
)
}
}
